const canvas=document.getElementById('dot-canvas');const ctx=canvas.getContext('2d');let pts=[];function resize(){canvas.width=innerWidth;canvas.height=Math.max(640,innerHeight*.92);pts=Array.from({length:120},()=>({x:Math.random()*canvas.width,y:Math.random()*canvas.height,r:Math.random()*1.8+.4,a:Math.random()*.45+.1,v:(Math.random()-.5)*.18}));}function draw(){ctx.clearRect(0,0,canvas.width,canvas.height);for(const p of pts){p.y+=p.v;if(p.y<0)p.y=canvas.height;if(p.y>canvas.height)p.y=0;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle='rgba(38,221,77,'+p.a+')';ctx.fill();}requestAnimationFrame(draw)}addEventListener('resize',resize);resize();draw();