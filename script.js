const canvas=document.getElementById("dot-canvas"),ctx=canvas.getContext("2d");let pts=[];function resize(){canvas.width=window.innerWidth;canvas.height=Math.max(700,window.innerHeight*.95);pts=Array.from({length:105},()=>({x:Math.random()*canvas.width,y:Math.random()*canvas.height,r:Math.random()*1.7+.35,a:Math.random()*.4+.08,v:(Math.random()-.5)*.12}));}function draw(){ctx.clearRect(0,0,canvas.width,canvas.height);for(const p of pts){p.y+=p.v;if(p.y<0)p.y=canvas.height;if(p.y>canvas.height)p.y=0;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle="rgba(50,230,92,"+p.a+")";ctx.fill();}requestAnimationFrame(draw)}window.addEventListener("resize",resize);resize();draw();

const form=document.getElementById("lead"),status=document.querySelector(".form-status");
if(form){form.addEventListener("submit",async e=>{e.preventDefault();status.className="form-status";status.textContent="Submitting your seat request…";const data=Object.fromEntries(new FormData(form).entries());try{const r=await fetch("https://formspree.io/f/REPLACE_WITH_YOUR_FORM_ID",{method:"POST",headers:{"Accept":"application/json","Content-Type":"application/json"},body:JSON.stringify(data)});if(!r.ok)throw new Error("submit");form.reset();status.className="form-status success";status.textContent="Your seat request has been received. We’ll share webinar details with you shortly."}catch(err){status.className="form-status error";status.textContent="We couldn’t submit the form yet. Please check the form endpoint configuration."}})}